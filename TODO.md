# TODO

 - Add current time and date in the right of the taskbar
 - Add option for outlining the window during resizing or moving (for slower computers or unaccelerated X servers)
 - Add options file (`options.conf` maybe)
 - Add changing colors (in `colors.conf` or smth)
 - See if there are any optimizations (it's not slow, it's just that optimization is always good.)
 - Fix issue where if a window is taking input where the corner/side of the window is taking input there (such as a text editor or image viewer), where the resize area is supposed to be, gets handed to the program, instead of Antiquity, meaning it is very hard to resize some programs, such as XTerm.
